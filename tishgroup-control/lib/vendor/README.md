# Shared TillFlow libs (vendored)

Copied from the monorepo `lib/` folder for standalone Vercel deploys.

Refresh after changing shared logic:

```bash
node scripts/sync-vendor-libs.mjs
```

Run from `tishgroup-control/` when the parent `../lib` exists.
